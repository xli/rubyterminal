#!/usr/bin/env ruby
begin
  require 'ruby_terminal'
rescue LoadError
  require 'rubygems'
  require 'ruby_terminal'
end

programfile = ARGV.shift
RubyTerminal.execute(programfile, ARGV, STDOUT) do
  exit
end
require programfile
